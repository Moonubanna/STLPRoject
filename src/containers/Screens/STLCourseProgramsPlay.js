import { connect } from 'react-redux'

import STLCourseProgramsPlay from '../../components/Screens/STLCourseProgramsPlay'
import {

} from '../../thunks'

const mapStateToProps = ({ AppUsers  }) => ({

})

const mapDispatchToProps = {
    
}

export default connect(mapStateToProps, mapDispatchToProps)(STLCourseProgramsPlay)