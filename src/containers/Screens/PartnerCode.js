import { connect } from 'react-redux'

import PartnerCode from '../../components/Screens/PartnerCode'
import {

} from '../../thunks'

const mapStateToProps = ({ AppUsers  }) => ({

})

const mapDispatchToProps = {
    
}

export default connect(mapStateToProps, mapDispatchToProps)(PartnerCode)